[project]
name = "llm-chat-agent"
version = "0.0.1"
description = "Sample agent demonstrating actual LLM calls with UiPath LLM Gateway"
requires-python = ">=3.11"
dependencies = [
    "uipath>=2.8.0, <2.9.0",
]

[dependency-groups]
dev = [
    "uipath-dev>=0.0.15",
]
