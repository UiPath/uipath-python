[project]
name = "event-trigger"
version = "0.0.1"
description = "Event Trigger Demo"
authors = [{ name = "John Doe", email = "john.doe@myemail.com" }]
dependencies = [
  "uipath"
]
requires-python = ">=3.11"

[dependency-groups]
dev = [
    "uipath-dev",
]
