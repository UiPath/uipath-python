[project]
name = "llm-chat-agent"
version = "0.0.1"
description = "Sample agent demonstrating actual LLM calls with UiPath LLM Gateway"
requires-python = ">=3.11"
dependencies = [
    "uipath",
]

[dependency-groups]
dev = [
    "uipath-dev",
]
