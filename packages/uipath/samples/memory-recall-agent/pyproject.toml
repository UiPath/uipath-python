[project]
name = "memory-recall-agent"
version = "0.0.1"
description = "UiPath coded agent sample that recalls context from a Memory Space"
authors = [{ name = "John Doe", email = "john.doe@myemail.com" }]
dependencies = [
    "uipath",
]
requires-python = ">=3.11"

[dependency-groups]
dev = [
    "uipath-dev",
]
