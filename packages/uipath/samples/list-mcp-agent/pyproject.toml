[project]
name = "list-mcp-agent"
version = "0.0.1"
description = "UiPath MCP agent"
authors = [{ name = "John Doe", email = "john.doe@myemail.com" }]
dependencies = [
    "uipath",
    "mcp>=1.28.1",
]
requires-python = ">=3.11"

[dependency-groups]
dev = [
    "uipath-dev",
]
