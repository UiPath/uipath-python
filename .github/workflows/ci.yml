name: Core CI

on:
  workflow_dispatch: {}
  push:
    branches:
      - main
    paths:
      - "pyproject.toml"
      - "src/**"
  pull_request:
    branches:
      - main
    paths:
      - "pyproject.toml"
      - "src/**"

jobs:
  lint:
    uses: ./.github/workflows/lint.yml

  test:
    uses: ./.github/workflows/test.yml
